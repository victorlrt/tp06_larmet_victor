import { CheckFormEMailDirective } from './check-form-email.directive';

describe('CheckFormEMailDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckFormEMailDirective();
    expect(directive).toBeTruthy();
  });
});
