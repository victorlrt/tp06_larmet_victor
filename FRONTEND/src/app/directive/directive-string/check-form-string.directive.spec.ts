import { CheckFormStringDirective } from './check-form-string.directive';

describe('CheckFormStringDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckFormStringDirective();
    expect(directive).toBeTruthy();
  });
});
