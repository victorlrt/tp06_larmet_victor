import { CheckFormNumberDirective } from './check-form-number.directive';

describe('CheckFormNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckFormNumberDirective();
    expect(directive).toBeTruthy();
  });
});
