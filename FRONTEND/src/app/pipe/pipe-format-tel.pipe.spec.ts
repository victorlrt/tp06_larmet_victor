import { PipeFormatTelPipe } from './pipe-format-tel.pipe';

describe('PipeFormatTelPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFormatTelPipe();
    expect(pipe).toBeTruthy();
  });
});
